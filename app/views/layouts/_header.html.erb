<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to fa_icon("home", text: "Home"), root_path %></li>
        <% if logged_in? %>
          <li><%= link_to fa_icon("clock-o", text: "WorkTimes"), work_times_path%></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= fa_icon "cogs" %> Settings <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to fa_icon("user", text: "Profile"), current_user %></li>
              <li><%= link_to fa_icon("cogs", text: "Account"), edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to fa_icon("sign-out", text: "Log out"), logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
