<p>
  <div class="btn-group">
    <%= link_to '変更',
     {:controller => "work_times", :action => "edit", :id => @work_time.id},
      class: "btn btn-primary btn-xs" %>
  </div>
</p>
<table class="table table-bordered">
  <tr>
      <th><%= fa_icon "calendar" %> 日付</th>
      <td><%= best_in_place @work_time, :work_date %></td>
  </tr>
  <tr>
      <th><%= fa_icon "clock-o" %> 始業時間</th>
      <td>
          <%= best_in_place @work_time, :start_time,
              :display_with => proc { |t| format_time(t) } %>
      </td>
  </tr>
  <tr>
      <th><%= fa_icon "clock-o" %> 終業時間</th>
      <td>
        <%= best_in_place @work_time, :end_time,
            :display_with => proc { |t| format_time(t) } %>
      </td>
  </tr>
  <tr>
      <th><%= fa_icon "sticky-note-o" %> 遅刻区分</th>
      <td>
        <%= best_in_place @work_time, :late,
            :display_with => proc { |h| @late_enum.key(h) } %>
      </td>
  </tr>
  <tr>
      <th><%= fa_icon "sticky-note-o" %> 早退区分</th>
      <td>
        <%= best_in_place @work_time, :leave,
            :display_with => proc { |h| @leave_enum.key(h) } %>
      </td>
  </tr>
  <tr>
      <th><%= fa_icon "sticky-note-o" %> 休日区分</th>
      <td>
        <%= best_in_place @work_time, :holiday,
            :display_with => proc { |h| @holiday_enum.key(h) } %>
      </td>
  </tr>
  <tr>
      <th><%= fa_icon "sticky-note-o" %> 備考</th>
      <td>
        <%= best_in_place @work_time, :note %>
      </td>
  </tr>
</table>
